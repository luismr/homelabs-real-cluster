# Cluster SSH and hosts configuration
# Copy or edit values as needed for your physical machines.

# SSH user and optional private key (key is optional; default agent/keys will be used if empty)
SSH_USER="ubuntu"
SSH_KEY=""   # e.g., /home/you/.ssh/id_ed25519 (leave empty to use default)

# Master node IP or hostname
MASTER_IP="192.168.7.200"

# Worker nodes IPs or hostnames (space-separated)
WORKER_IPS="192.168.7.201 192.168.7.202 192.168.7.203"

# Optional friendly names for workers (space-separated; must match count of WORKER_IPS)
# If empty, names will default to worker1 worker2 worker3 by order
WORKER_NAMES="worker-1 worker-2 worker-3"

# Optional: extra SSH options (appended)
# SSH_EXTRA_OPTS="-o ProxyJump=..."
SSH_EXTRA_OPTS=""


